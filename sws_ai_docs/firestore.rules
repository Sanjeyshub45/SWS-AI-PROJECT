rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open rules for assessment — tighten for production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
